url: "mosca.net.br" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: "" # the subpath of your site, e.g. /blog
title: "Blog do Mosca" # the name of your site, e.g. ACME Corp.

defaults:
  -
    scope:
      path: "" # aplica a todo o site
    values:
      layout: default
  -
    scope:
      path: "/medicina"
    values:
      category: medicina

permalink: /:title
